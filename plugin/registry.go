package plugin

// TODO: Registry maps plugin type names to factories and builds plugin
// instances from configuration. Registration is safe for concurrent use;
// Default is the package-level registry used by built-in plugins.
